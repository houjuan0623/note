# connect\&bind\&listen\&accept

## bind：构造数据结构之属性融合

一个新创建的套接字虽然存在于某个地址族（如 `AF_INET`）中，但它是匿名的，没有被分配任何具体的地址。

`bind()` 系统调用的作用就是解决这个问题，它将一个本地地址（IP 和端口号）与一个套接字描述符关联起来 。对于服务器而言，这是在接受连接之前必须执行的关键步骤 。

当内核收到一个 `bind()` 调用时，它会执行一系列严格的验证：

* 描述符有效性：检查 `sockfd` 是否是一个指向套接字的有效文件描述符 (`EBADF`, `ENOTSOCK`) 。  &#x20;
* 地址有效性：确保提供的地址对于套接字的地址族是有效的，并且该套接字尚未被绑定 (`EINVAL`) 。  &#x20;
* 权限检查：验证进程是否拥有绑定到所请求端口的权限。例如，在类 Unix 系统中，绑定到 1024 以下的端口通常需要超级用户权限 (`EACCES`) 。  &#x20;
* 地址可用性：检查该 IP 地址和端口号的组合是否已被其他套接字占用 (`EADDRINUSE`)。这是 `bind()` 调用中最常见的错误 。

如果所有检查都通过，内核会将本地地址和端口信息更新到内部的 `struct sock` 结构体中。这一操作将抽象的套接字对象锚定到网络上的一个具体坐标，使其能够被客户端发现和连接。

## listen：构造数据结构之属性标记

设置套接字的属性为被动监听套接字。

从用户进程的角度看，`listen()` 是一个非常简单的、非阻塞的操作 。它的唯一目的，是将一个已绑定的套接字标记为“被动”套接字，表明它愿意接受传入的连接请求 。

这个调用将套接字的角色从一个可以发起连接（通过 `connect()`）的主动实体，转变为一个只能接收连接的被动实体。它是调用 `accept()` 的先决条件 。

在 `listen()` 调用期间，内核执行的最重要的动作是为这个监听套接字分配和初始化两个用于管理传入连接请求的关键队列 。

### 监听套接字的两个队列

现代 Linux 内核使用两个独立的队列来管理监听套接字的传入 TCP 连接 。这种双队列系统取代了早期单一队列的模型，为处理连接建立过程提供了更精细的控制。

[可以提前参考TCP相关的文章。](tcp.md)

**SYN 队列（未完成连接队列）：**

* 目的：此队列（通常以哈希表实现）用于存储已完成 TCP 三次握手第二步的连接请求。当服务器收到客户端的 SYN 包时，它会回复一个 SYN-ACK 包，并在 SYN 队列中为此连接请求创建一个轻量级的数据结构 (`struct inet_request_sock`) 。此时，连接处于`SYN_RECV` 状态。
* 大小：该队列的大小由一个全局内核参数 `/proc/sys/net/ipv4/tcp_max_syn_backlog` 控制 。

**Accept 队列（已完成连接队列）：**

* 目的：此队列用于存放已经完成完整三次握手、处于 `ESTABLISHED` 状态的连接。这些连接正在等待应用程序通过 `accept()` 系统调用来取走 。当服务器收到来自客户端的最终 ACK 包时，内核会创建一个完整的套接字结构 (`struct inet_sock`)，将连接从 SYN 队列移至 Accept 队列，此时服务器端的连接状态变为 `ESTABLISHED` 。  &#x20;
* 大小：该队列的大小由传递给 `listen()` 系统调用的 `backlog` 参数直接控制 。然而，这个值会被全局内核参数`/proc/sys/net/core/somaxconn` 静默地限制在其上限之内 。

> 从单一队列到双队列的演进，是内核网络栈设计思想成熟的体现。
>
> 早期的 BSD 模型使用单一队列，`backlog` 参数控制着未完成 (`SYN_RECV`) 和已完成 (`ESTABLISHED`) 连接的总和 。这导致了应用性能和网络性能的紧密耦合：
>
> * 一个处理缓慢的应用程序（不及时调用`accept()`）可能导致队列被 `ESTABLISHED` 状态的连接占满，从而无法接受新的连接请求；
> * 反之，一个高延迟的网络可能导致队列被 `SYN_RECV` 状态的连接占满，使得一个处理迅速的应用程序也无事可做 。
>
> Linux 的双队列模型则将这两个问题解耦 。应用程序开发者根据其处理`accept()` 的速度来调整 `backlog` 参数，而系统管理员则根据网络状况（如 RTT 和预期的连接速率）来调整 `tcp_max_syn_backlog` 。
>
> 这种关注点分离的设计，允许对系统进行更精确、独立的调优，因为它深刻认识到高性能服务器的瓶颈可能来自**应用层**，也可能来自**网络层**。
>
> * Accept 队列溢出：如果应用程序调用 `accept()` 的速度不够快，Accept 队列将会被填满。当队列满时，内核的行为由 `/proc/sys/net/ipv4/tcp_abort_on_overflow` 参数决定。如果设置为 0（默认值），内核会简单地丢弃客户端发来的最终 ACK 包。这会导致服务器端的连接停留在 SYN 队列中，而客户端却认为连接已建立，最终造成半开连接和客户端超时 。如果设置为 1，内核会发送一个 RST 包，使客户端连接立即失败 。一个满的 Accept 队列也会导致内核开始丢弃新传入的 SYN 包 。  &#x20;
> * SYN 队列溢出与 SYN 洪水攻击：SYN 队列是 SYN 洪水 (SYN Flood) DDoS 攻击的主要目标。攻击者发送大量 SYN 包（通常来自伪造的 IP 地址），但从不发送最终的 ACK。这会用半开连接填满服务器的 SYN 队列，消耗服务器内存，并阻止合法用户建立连接 。  &#x20;
> * 内核防御：SYN Cookies：为了对抗 SYN 洪水攻击，内核采用了一种名为 SYN Cookies 的机制，由 `/proc/sys/net/ipv4/tcp_syncookies` 控制 。当 SYN 队列已满且 SYN Cookies 被启用时，服务器不会在 SYN 队列中创建条目。相反，它会将连接的必要信息（如 MSS、时间戳等）编码到一个经过特殊计算的序列号中，并将其作为 SYN-ACK 包的初始序列号发送出去。合法的客户端会回复一个 ACK，其确认号为 `序列号 + 1`。服务器收到此 ACK 后，可以通过密码学方法验证其有效性，并从中解码出连接信息，然后直接将连接放入 Accept 队列，整个过程完全无需在 SYN 队列中存储任何状态 。

**TCP 连接队列调优的关键内核参数**

| 参数                               | 描述                                                | 默认值 (典型)          | 影响                                                         |
| -------------------------------- | ------------------------------------------------- | ----------------- | ---------------------------------------------------------- |
| `net.core.somaxconn`             | Accept 队列大小的硬性上限。`listen()` 中的 `backlog` 值不能超过此值。 | 4096 (Linux 5.4+) | 直接限制了可以等待应用程序处理的已完成连接数。在高并发负载下，过低的值可能导致连接被丢弃，即使应用程序处理速度很快。 |
| `net.ipv4.tcp_max_syn_backlog`   | SYN 队列中可以容纳的半开连接的最大数量。                            | 1024 (可变)         | 限制了服务器处理新连接请求的能力。在流量高峰期，过低的值可能导致合法的 SYN 请求被丢弃。             |
| `net.ipv4.tcp_syncookies`        | 启用/禁用 SYN Cookie 机制以防御 SYN 洪水攻击。                  | 1 (需要时启用)         | 对 DDoS 防御至关重要。启用后，即使 SYN 队列逻辑上已满，服务器也能处理连接请求，防止拒绝服务。       |
| `net.ipv4.tcp_abort_on_overflow` | 决定当一个连接到达一个已满的 Accept 队列时的行为。                     | 0 (丢弃 ACK)        | `0` 可能导致客户端超时和重传。`1` 会发送 RST，导致快速失败，这对于负载均衡器和服务发现可能更有利。    |
| `net.ipv4.tcp_synack_retries`    | 在放弃一个半开连接之前，重传 SYN-ACK 的次数。                       | 5                 | 决定一个半开连接在 SYN 队列中停留的时间。较低的值能更快地清理过时条目，但在慢速或有损网络上可能会丢失连接。   |



## accept：从内核队列到用户应用

`accept()`：**从队列中取出已完成的连接。**

`accept()` 系统调用是服务器从内核的 Accept 队列中获取一个已完全建立的连接的机制 。

它从监听套接字 (`sockfd`) 的队列中提取第一个挂起的连接。该调用还可以填充一个`sockaddr` 结构，其中包含连接对端的地址信息，从而使服务器能够识别客户端 。如果队列为空，其行为取决于套接字是阻塞还是非阻塞的 。

* 阻塞：`accept()`调用会暂停你的程序，让它“睡眠”。程序会一直卡在这里，直到有一个新的连接完成TCP握手并进入队列。这对于单线程、顺序执行的服务器来说很简单，但它会阻止服务器在等待时执行任何其他工作。
* 非阻塞：`accept()`调用不会暂停程序。它会立即返回一个错误码（通常是`EAGAIN`或`EWOULDBLOCK`），这种行为对于事件驱动的服务器至关重要。服务器可以尝试调用`accept()`，如果没有连接就绪，它可以继续处理现有连接上的 I/O 或执行其他任务，从而防止整个服务器停滞。

`accept()` 调用最核心、也最容易被误解的一点是，它并不会改变监听套接字本身，而是创建一个全新的套接字，并返回这个新套接字的文件描述符 。  &#x20;

* 监听套接字 (`sockfd`)：这个原始套接字保持不变，并继续处于监听状态。它的唯一工作是继续监听和排队来自其他客户端的新的连接请求 。它就像一个连接工厂。  &#x20;
* 连接套接字（新的 FD）：这个新创建的套接字处于 `ESTABLISHED` 状态，用于与刚刚接受的特定客户端进行所有后续的通信（`read()`, `write()`） 。它代表一个专用的、一对一的通信信道。  &#x20;

这种架构上的分离，使得单个服务器进程能够处理成千上万的并发客户端连接。主线程或主进程可以在一个循环中调用 `accept()`，不断创建新的连接套接字，然后将这些套接字分派给工作线程或在事件循环中进行管理。

通过将非阻塞的 `accept()` 与 I/O 多路复用机制（特别是 `epoll`）相结合，服务器可以将监听套接字像其他任何文件描述符一样对待。服务器将监听套接字添加到其 `epoll` 集合中。当一个新连接完成三次握手并被放入 Accept 队列时，内核会将该监听套接字标记为“可读”。随后，`epoll_wait()` 调用会返回，并指示监听套接字上有事件发生。服务器知道，监听套接字上的“可读”事件意味着现在可以调用 `accept()` 而不会被阻塞。这种机制将新连接的处理和现有数据的 I/O 统一在单个事件循环中，允许单线程高效地管理成千上万个连接的整个生命周期。这是像 Nginx 这样的高性能服务器背后的核心原理。

## connect：主动建立连接

调用connect传递套接字地址结构的方向是：用户进程->内核进程

从用户进程的角度来看，`connect()` 是发起与远程服务器连接的系统调用 。应用程序提供套接字描述符和服务器的地址，之后的一切都交由内核处理。

如果客户端套接字没有被显式地绑定到一个本地地址和端口，内核会在 `connect()` 调用期间，自动从 `/proc/sys/net/ipv4/ip_local_port_range` 指定的范围内为其分配一个临时端口 。

connect调用将激发TCP三次握手的过程（可以参考[文章](tcp.md)）：

1. 第一步：SYN：客户端内核构建并发送一个设置了 SYN (同步) 标志的 TCP。此时，客户端套接字转换到`SYN_SENT` 状态 。
2. 第二步：SYN-ACK：服务器内核在收到 SYN 包后，如果 SYN 队列有空间，会将连接请求放入队列，并回复一个同时设置了 SYN 和 ACK 标志的 TCP 段。服务器套接字转换到`SYN_RECV` 状态 。
3. 第三步：ACK：客户端内核收到 SYN-ACK 后，验证确认号，并发送一个设置了 ACK 标志的最终段。服务器在收到这个最终的 ACK 后，也转换到`ESTABLISHED` 状态 。

### 阻塞与非阻塞的 connect

阻塞与非阻塞模式的 `connect()` 调用，其行为差异巨大，这体现了内核与用户空间应用程序之间责任划分的根本性转变。

* 阻塞 `connect()` (默认)：在默认的阻塞模式下，发送初始 SYN 包后，用户进程将被内核置于睡眠状态。`connect()` 系统调用直到三次握手成功完成（返回 0）或失败（返回 -1 并设置 `errno`，如 `ETIMEDOUT`、`ECONNREFUSED`）才会将控制权返还给应用程序 。这种模型编程简单，但对于需要保持响应性的应用（如 GUI 程序或管理大量出站连接的服务器）来说完全不适用。
* 非阻塞 `connect()`：当一个套接字被标记为非阻塞（通过 `fcntl` 或 `O_NONBLOCK`）时，`connect()` 的行为截然不同。
  * 它通过发送 SYN 包来启动握手过程，然后立即返回到应用程序，而不会等待响应 。  &#x20;
  * 此时的返回值是 -1，并且 `errno` 被设置为 `EINPROGRESS` 。这并非一个真正的错误，而是一个信号，表示连接正在异步建立中。
  *   应用程序必须使用 I/O 多路复用机制（如 `select()`, `poll()` 或 `epoll()`）来监控该套接字描述符。当连接成功建立时，该套接字会变为可写状态（这种状态的改变会通过多路复用机制通知到应用程序） 。

      > 1. 调用`connect`函数，它会立即返回，而不会等待连接建立完成。
      > 2. 由于连接过程（TCP三次握手）需要时间，所以在`connect`立即返回时，连接通常还没有建立好。此时，它会返回一个特殊的错误码`EINPROGRESS`，告诉你：“连接请求我已经发出，正在进行中，但你得自己稍后回来查看结果。”
      > 3. 如何得知连接结果：使用`select`, `poll`, `epoll`这样的I/O多路复用机制。


  * 在多路复用调用返回后，应用程序必须通过调用 `getsockopt()` 并使用 `SO_ERROR` 选项来确认最终结果。如果 `SO_ERROR` 的值为 0，表示连接成功。如果非零，它将包含阻塞 `connect()` 本应返回的错误码（如 `ECONNREFUSED`） 。

非阻塞 I/O 模型代表了内核与用户空间应用程序之间分工的根本性转变。在阻塞模式下，内核全权管理连接尝试的整个状态机（等待、超时、重试），并且只在最终成功或失败时唤醒进程，进程完全是被动的。而在非阻塞模式下，内核只负责发起动作。`EINPROGRESS` 的返回码实际上是在告诉应用程序：“我已经启动了这个过程，但现在由你来监控其进展并确定最终结果。” 应用程序现在必须主动管理连接状态，将套接字集成到事件循环中，等待可写事件，然后通过 `getsockopt` 进行最终检查。这种模式将一个同步阻塞的系统调用转变为一个异步的、状态驱动的操作。这是获得并发性和响应性所付出的代价：**以应用程序复杂度的增加，换取了更大的控制力和效率。**

