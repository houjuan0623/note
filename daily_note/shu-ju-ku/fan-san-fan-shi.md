# 反三范式

在数据库设计中，为了考虑性能的因素，会适度地进行反三范式化。

### 范式化的目的

数据库的范式化的目的是减少数据冗余并保证数据的一致性（这里的一致性是指不同表中相同字段应该是一致的）。

### 范式化的缺点

当数据高度范式化的时候，查询数据需要通过 `JOIN` 操作连接多个表。当数据量特别巨大或者并发请求很高的时候，大量的 `JOIN` 操作会消耗大量的CPU和IO资源。

x

### 范式化的优点

